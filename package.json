{
  "name": "myeditor",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/playground",
    "build": "npm run build --workspace=packages/editor",
    "typecheck": "tsc --build",
    "test": "npm run test --workspace=packages/editor"
  },
  "devDependencies": {
    "rollup-plugin-visualizer": "^7.0.1"
  }
}
